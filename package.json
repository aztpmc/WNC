{
  "name": "number-financial-tools",
  "version": "0.1.0",
  "private": true,
  "description": "Offline-first number, amount, and trade-finance reference tools.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test tests/core/*.test.js",
    "test:e2e": "playwright test",
    "fixture:generate": "node tests/support/generate-golden-fixture.js",
    "fixture:compare": "node tests/support/compare-golden-fixture.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "vite": "^5.4.21"
  }
}
